spring:
  datasource:
    url: jdbc:h2:mem:powerplanttest
    driverClassName: org.h2.Driver
    username: sa
    password: test
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create